import type { TextualApparatus } from '@/archive/content'

/** Sections 03–08 authored against the implemented mechanic. */
export const apparatus: Partial<TextualApparatus> = {
  whatTheMachineDid:
    'The console suspends the existence-thesis of the natural attitude stepwise rather than all at once: each [BRACKET] peels one mode of givenness — the asserted object, the single profile, the co-given unseen sides, the retentional trail and protentive ghost of the temporal flow, the bodily “here” that indexes every view, and finally identity itself, displayed as a synthesis bar that fills because the user achieves it, not because the object supplies it.',
  whatTheMachineDistorted:
    'It makes the epoché look like a sequence of six clicks, i.e. a feature tour. Husserl’s reduction is a change of overall attitude, not six sub-modes; and the console’s tidy layer order suggests constitution can be paused at will, when the analysis is a description of an already-operating achievement. The chair-graphics are also, irreducibly, one more profile among the profiles it claims to enumerate.',
  whyDistortionMatters:
    'The machine’s central correct decision — bracketing is not doubting existence — would be undone if the apparatus ended with the chair winking out of being. Hence the final line: the object remains, as constituted. The risk the apparatus cannot fully retire is that users leave thinking phenomenology is introspection with labels; the take-back questions exist for that reader.',
  competingReading:
    'A objectivist reading holds that the profiles are simply information-theoretic projections of a 3-D model and the “horizon” is occlusion — no constitution, just rendering. The apparatus neither confirms nor refutes this; it observes that the rendering account itself takes for granted the identity-across-views the console asks the user to perform.',
  implementationNotes:
    'Rotation is discrete (eight buckets) to keep profiles honest — continuous morphing would fake the givenness of intermediate profiles. The retention strip is the console log itself, dimmed but persistent: the machine performs what it describes. Interpretation modes (static, temporal, embodied) alter which layers are foregrounded; see the mode selector after first completion.',
}
