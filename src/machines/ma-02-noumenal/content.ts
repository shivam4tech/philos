import type { TextualApparatus } from '@/archive/content'

/** Sections 03–08 authored against the implemented mechanic. */
export const apparatus: Partial<TextualApparatus> = {
  whatTheMachineDid:
    'The inspector offers a raw-object pane. Every attempt to open it returns another representation — texture, figure, extent, cause, each already structured by the categories of the asking — nested inside the previous one, rendered as a parody of a browser’s elements panel. After six pans, the pane beneath is reported as NULL, which the machine notes is itself a category. The inspection is withheld.',
  whatTheMachineDistorted:
    'It stages the thing-in-itself as a hidden file behind visible files — an ontological basement one layer down. Kant’s critical turn is not that reality is hidden but that the demand for an unmediated view is incoherent; the parody pane keeps the demand alive to mock it, which risks keeping it alive, full stop.',
  whyDistortionMatters:
    'The developer-tools conceit is funny and unstable: someone will press F12 for real and find, beneath the pane, an actual DOM — an unmediated object for the inspector’s mediated ones. The apparatus therefore must rely on its verdict line to restore the point: the failure is epistemic structure, not engineering. Where it fails to land, the machine teaches the opposite of B xxvi.',
  competingReading:
    'A two-world realist reading accepts the basement: the thing-in-itself exists, unknown. A two-aspect reading denies the basement: the same object considered without our forms of cognition. The NULL return is deliberately ambiguous between them — unknowable thing, or empty abstraction?',
  implementationNotes:
    'Representations are authored in order of increasing mediation; the nesting depth enforces the regress. The inspector’s own title is clickable thrice before filing a record (UR-016): attempting to inspect the inspector yields only more inspector.',
}
