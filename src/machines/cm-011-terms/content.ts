import type { TextualApparatus } from '@/archive/content'

/** Sections 03–08 authored against the implemented mechanic. */
export const apparatus: Partial<TextualApparatus> = {
  whatTheMachineDid:
    'A service agreement whose load-bearing terms are clickable. Each definition is written entirely in other terms; opening a definition nests further definitions beside it, and a trace graph counts terms and references while conspicuously failing to find a ground floor. Definitions revise earlier ones retroactively (RESPONSIBILITY returns marked “revised in light of MEANING”), and the ACCEPT button retreats through relabels — of what? pending context? deferred — until the user consents from inside the graph, having reached no ground.',
  whatTheMachineDistorted:
    'It renders différance as a UI navigation pattern, which domesticates it: a stack of popovers is navigable and closable, while the movement it stands in for is not an interface state but the condition of interface (and everything else). The machine also closes with a granted consent — the one thing the analysis most refuses to guarantee.',
  whyDistortionMatters:
    'The risk is teaching the caricature “words mean nothing.” The graph is built to block it: every definition here functions, consents are processed, the contract operates — meaning is not absent but constituted differentially, with constitutive deferral. The revision notes exist because that constitution is unstable, not because it is absent; if the user leaves with nihilism, the apparatus has failed in the precise direction it was designed against.',
  competingReading:
    'A analytic reading treats the regress as benign: definitions stop when mutual understanding suffices, and unresolved context is an engineering problem, not a metaphysical one. The apparatus registers the challenge honestly — its own ACCEPT button, after all, works — and leaves the tension standing rather than resolving it by fiat.',
  implementationNotes:
    'The term graph is authored data (unit-tested for reachability and acyclicity of individual definitions’ references). Revisions trigger on reading order, not on chronology. The self-definition field records typing the term’s own name as an unauthorized procedure; the circle is praised, then filed.',
}
